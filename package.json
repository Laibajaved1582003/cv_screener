{
  "name": "cv-screener",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm run build"
  },
  "dependencies": {
    "mammoth": "^1.6.0",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "@google/generative-ai": "^0.21.0"
  }
}
